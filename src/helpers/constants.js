const basketPosition = {
  bottom: 550,
  left: -110
};

const bottomRange = {
  min: 260,
  max: 350
};

const leftRange = {
  min: 30,
  max: 150
};

const transitionRange = {
  min: 400,
  max: 700
};

const dropPositionRange = {
  min: 15,
  max: 30
};

const appleCountRange = {
  min: 2,
  max: 2
};

export {
  basketPosition,
  bottomRange,
  leftRange,
  transitionRange,
  appleCountRange,
  dropPositionRange
};
